/** Supported sprite dimensions. */
export const SIZES = Object.freeze([32, 64, 128]);

/** Palette size (indexed 0–255). */
export const PALETTE_SIZE = 256;

/** Default format version for load/save. */
export const FORMAT_VERSION = 1;
