/** Pencil: draw with current color index. */
export const PENCIL = 'pencil';

/** Eraser: draw with index 0. */
export const ERASER = 'eraser';

export const TOOLS = Object.freeze([PENCIL, ERASER]);
